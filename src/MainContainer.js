import React from 'react';

function MainContainer() {
    return (
        <div className="MainContainer" style={{ flexGrow: 1 }}>
            <h2>Main Container</h2>
        </div>
    );
}

export default MainContainer;
